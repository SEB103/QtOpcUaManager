; Presence of this file selects portable mode: the application keeps all user
; data (config/, data/, logs/) inside this directory instead of the per-user
; Windows locations. Added only to the portable ZIP; the installer never writes it.
[Portable]
enabled=true
