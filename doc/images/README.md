# Documentation images

Screenshots and figures referenced by the QDoc manual pages (`doc/manual/<lang>/*.qdoc`)
via `\image` / `\inlineimage`. This directory is registered with QDoc as `imagedirs`.

Place PNG screenshots of the running application here (for example
`client-browse.png`, `server-studio.png`). Keep file names language-neutral so every
translated manual can reference the same image.
